package superlink.udpbind.client.customer;

public class customFactory {

//    public static Bindrec newBind(UserRequest userRequest){
//        //int id= UDPclient.mainDataQueue.newUserId(userRequest);
//        UserContext userContext = UDPclient.mainDataQueue.createQueue(userRequest.username,userRequest.toaddress, userRequest.toport);
//        //return new Bindrec(blockingQueue,userRequest.username);
//    }
}
